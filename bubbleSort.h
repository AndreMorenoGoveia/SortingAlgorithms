/* Função que faz o bubblesort para organizar um vetor em ordem
   crescente ou decrescente

   complexidade do sort - O(n^2) */


/* Função que troca entre si dois valores de um vetor */
static void troca(int* a, int* b);



/* Ordena o vetor carregando os maiores ou menores valores para
   o fim do vetor multiplas vezes */
void bubbleSort(int* v, int n);